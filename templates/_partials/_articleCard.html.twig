<div class="container-fluid bg-white mb-4 rounded-2">
    <div class="row py-3">
        
        <div class="col-4">
            <img src="{{ vich_uploader_asset(article, 'imageFile') }}" class="card-img-top rounded-2">
        </div>

        <div class="col-8">
            <h5>{{ article.title }}</h5>

            <p>{{ article.content|striptags|u.truncate(120, ' ...')|raw}}</p>

            <div>
                <a href="{{ path('app_blog_article', {'article' : article.id}) }}">Lire la suite...</a>
            </div>
        </div>

    </div>
</div>